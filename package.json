{
  "name": "provenance",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,magenta \"npm run dev -w @provenance/api\" \"npm run dev -w @provenance/web\"",
    "start": "npm run dev",
    "build": "npm run build -w @provenance/shared && npm run build -w @provenance/api && npm run build -w @provenance/web",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20"
  }
}
